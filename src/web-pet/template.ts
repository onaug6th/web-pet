const container: string = `<div class="web-pet"></div>`;

const pet: string = `<div class="pet"></div>`;

const menu: string =
    `<div class="pet-menu">
        <ul>
            <li>聊天</li>
        </ul>
    </div>`;

const chat: string =
`<div class="pet-chat">
    <input />
</div>`;

export {
    container,
    pet,
    menu,
    chat
}