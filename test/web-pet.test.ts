import WebPet from "../src/web-pet/web-pet";
import { expect } from 'chai';

const webPet = new WebPet();

describe('工具类测试套件', function () {

    // it(`WebPet应当能够实例化`, function () {
    //     const webPet = new WebPet();
    //     expect(webPet.constructor).to.be.equal(WebPet);
    // });

});
